# JS
node_modules/
dist/
dist-firefox/
coverage/
*.tsbuildinfo

# env
.env
.env.*
!.env.example
# Dev-only defaults, no secrets: Cloudflare's documented always-pass Turnstile test key, so
# the guest sign-in path is exercisable on a fresh clone's dev stack (never a real site key).
!apps/web/.env.development

# Swift / Xcode (apps/ios)
.build/
DerivedData/
*.xcuserstate
xcuserdata/

# iOS harness scratch (apps/ios/scripts): pidfiles and per-run derived data, never
# committed. The room proof's screenshots live in the OS tmpdir, outside the repo.
apps/ios/scripts/.integration.pid
apps/ios/scripts/.room.pid
apps/ios/scripts/.room-derived/

# OS / editor
.DS_Store
.claude/

# Scratch design-review screenshots (verification artifacts, never committed).
design-shots/

# Xcode CLI builds without a -derivedDataPath land here; build output, never committed.
apps/ios/Crossy/build/
