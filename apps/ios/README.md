# apps/ios (placeholder)

Wave 0.2b adds a Swift package here containing the XCTest vector runner — it consumes
the same JSON under `vectors/` as the TypeScript runner — plus the macOS CI job
path-filtered to `apps/ios/**` and `vectors/**`. The SwiftUI app itself is Phase 4
(M5): Canvas grid renderer, `URLSessionWebSocketTask` with the shared reconnect state
machine, native Sign in with Apple. See ROADMAP.md.
