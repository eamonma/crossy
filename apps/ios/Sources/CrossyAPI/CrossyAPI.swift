// CrossyAPI (AD-2: adapter; imports CrossyProtocol). REST client with the PROTOCOL.md
// §12 error vocabulary, auth session, Keychain, issuer-pinned token handling
// (deploy/README.md). Implements the store's TokenProvider port. Fills in Phase I1d.
