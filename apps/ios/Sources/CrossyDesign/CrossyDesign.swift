// CrossyDesign (AD-2: adapter; imports Foundation only). Tokens: the two grounds
// (Grounds.swift), the twelve-color roster and its FNV-1a indexing (IdentityRoster,
// IdentityHash; a cross-client contract, root DESIGN.md §8), type scale, motion
// constants, and the ID-1 attribution switches. Colors are data (RGBColor); SwiftUI
// construction lives in CrossyUI. Shared with the widget extension.
