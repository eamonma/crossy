// CrossyUI (AD-2: adapter; imports CrossyStore, CrossyDesign). SwiftUI views: the
// Canvas grid, key deck, chrome, haptics. Views are pure functions of the store's
// render model (INV-10). Fills in Phase I2.
