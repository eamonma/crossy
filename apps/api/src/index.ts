// Core API: stateless modular monolith (DESIGN.md §7) — identity & membership,
// puzzle catalog (owns the ingestion ACL), games, archive. Walking-skeleton slice
// lands in Wave 2.1b; module boundaries are enforced by the root boundary rules.
export {};
