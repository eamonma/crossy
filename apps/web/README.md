# apps/web (placeholder)

Wave 2.1d scaffolds the Vite React SPA here, in the same PR as the WS codec and store
so the template never sits half-wired. Store contract: client reducer + optimistic
overlay + connection state machine per PROTOCOL.md §§7–8, driven by the client-store
vectors. See ROADMAP.md.
